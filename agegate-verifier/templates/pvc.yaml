apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: agegate-data
  namespace: agegate
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: local-path
